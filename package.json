{
  "name": "remagine",
  "version": "1.0.0",
  "description": "Ask one question, get answers from 8 labeled AI personas (all backed by a single Groq API key) plus a summary.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
